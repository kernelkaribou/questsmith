services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - PUID=1000
      - PGID=1000
      - PIN=1234
      - DATABASE_PATH=/app/data/questsmith.db
    restart: unless-stopped
