services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_APP=app
      - FLASK_ENV=production
      - DATABASE_PATH=/app/data/questsmith.db
      - ADMIN_PIN=1234
    restart: unless-stopped
